# Specs

Feature specs and requirements docs.

- Copy `_template.md` to start a new spec
- Name files in kebab-case: `feature-user-auth.md`, `feature-notifications.md`
- Commit the spec after alignment: `docs: add spec for [feature]`
